# 生产环境配置
# 敏感信息从外部文件加载（生产服务器路径）
spring:
  config:
    import: optional:file:./config/secrets-prod.properties

# 生产环境关闭SQL日志
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.nologging.NoLoggingImpl
