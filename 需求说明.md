# 系统门户管理 - 开发需求文档

> **数据库**：MySQL 5.7  
> **后端框架**：Spring Boot 2.7.18 + Apache Maven 3.8.1  
> **前端技术**：原生 HTML5 + CSS3 + JavaScript（ES6+），无需 Node/Vue 构建  
> **JDK 版本**：OpenJDK 1.8  
> **Maven 版本**：Apache Maven 3.8.1  

---

## 1. 需求目的

构建一个**统一系统门户管理平台**，实现以下目标：

- 用户登录后进入门户首页，以**卡片形式**展示各子系统入口；
- 点击卡片进入对应子系统，各子系统拥有独立的页面与业务功能；
- **前后端完全分离**：前端为静态页面，后端提供 RESTful API；
- 支持**多子系统权限管控**：用户只能看到并进入已授权的子系统；
- 支持**角色 + 用户**两级权限分配，便于平台管理员与各子系统管理员协作；
- 各子系统使用**独立数据库**，便于扩展与维护。

---

## 2. 开发说明

- 代码完全分离，结构清晰，模块职责单一。
- 前端页面美观、风格统一，采用响应式布局适配 PC 与移动端。
- 可扩展性强：新增子系统只需增加数据库、后端模块、前端 `systems/` 目录及门户卡片配置。
- 门户首页卡片列表上方提供**搜索功能**（按系统名称、描述模糊匹配）。
- 门户首页暂时包含 4 个子系统卡片：用户管理系统、**订单商城**、数据库管理系统、日志分析平台。每个卡片代表一个独立系统，在 `front/systems/` 下分别维护各自的 HTML、JS、CSS。
- 所有子系统前端页面风格保持一致（侧边栏布局、统计卡片、表格、弹窗、Toast 提示等）。
- 数据库 DDL 及初始化数据放在 `backend/portal-main/src/main/resources/DDL.sql`。
- 敏感配置（数据库密码、JWT 密钥）通过 `backend/config/secrets-dev.properties` 外部加载，不提交到版本库。

---

## 3. 系统说明

### 3.1 门户首页

| 功能 | 说明 |
|------|------|
| 登录校验 | 未登录跳转 `login.html` |
| 子系统卡片 | 调用 `GET /api/subsystems/my`，仅展示当前用户有权限且**已启用**的子系统 |
| 搜索 | 前端本地过滤卡片（名称、描述） |
| 进入子系统 | 点击卡片跳转 `sys.url` 配置的相对路径 |
| 停用系统 | 状态为停用的卡片灰色显示，不可进入 |
| 管理员操作 | 平台管理员 / 子系统管理员可见「新增子系统」及卡片上的编辑、启停、删除按钮 |

**管理员与普通用户卡片差异：**

- **普通用户**：只看到有 `login` 及以上权限且启用的子系统；
- **管理员**（`PLATFORM_ADMIN` / `SUBSYSTEM_ADMIN`）：看到全部子系统（含停用），可进行门户级子系统 CRUD。

### 3.2 用户管理系统（USER_MGMT）

**定位**：管理全平台登录用户、角色及子系统访问权限。

**访问控制：**

- 需拥有用户管理子系统的 **login** 权限方可进入；
- 用户信息的增删改、导入导出、角色/权限分配等写操作，需为 **平台管理员** 或 **子系统管理员**（前端 `isAdmin()` + 后端 `@PreAuthorize` 双重控制）。

**功能模块：**

| 页面 | 路径 | 功能 |
|------|------|------|
| 用户管理 | `index.html` | 用户列表、搜索/状态筛选、前端分页；管理员可新增/编辑/删除/启停/重置密码；Excel 导入导出 |
| 角色管理 | `roles.html` | 角色 CRUD、启停 |
| 用户角色分配 | `role-assign.html` | 为角色批量分配用户（覆盖式） |
| 查询用户角色 | `user-roles.html` | 按用户查询其关联角色 |
| 子系统权限分配 | `perm-menu.html` | 按角色 / 按用户两个 Tab，为指定子系统分配 `login` / `query` / `admin` 权限 |
| 系统权限查看 | `perm-view.html` | 查看用户有效权限（直接分配 + 角色继承合并） |
| 数据权限管理 | `perm-data.html` | **占位页，功能开发中** |

**用户管理补充能力：**

- 个人信息页 `profile.html`：查看/修改姓名、邮箱、手机、密码；展示角色与有效子系统权限；
- 注册接口 `POST /api/auth/register`：创建用户并绑定角色（管理场景使用）；
- 启动时 `PasswordInitializer` 自动将 DDL 中 `INIT:` 前缀的明文密码加密为 BCrypt。

### 3.3 订单商城子系统（ORDER_MGMT）

**定位**：面向门户用户的**在线商城 + 订单履约**子系统。普通用户可浏览商品、加购、结算、查看自己的订单与物流；管理员可管理商品、分类、全部订单及统计数据。

**分页策略**：列表类接口（商品、订单、用户等）**后端仅做筛选并返回全量列表**；表格/卡片分页一律在**前端** `slice` 实现。

#### 3.3.1 页面与功能

| 模块 | 页面 | 路径 | 功能 |
|------|------|------|------|
| 商城 | 商品列表 | `index.html` | 分类/关键词筛选；前端分页；加入购物车 |
| 商城 | 购物车 | `cart.html` | 数量修改、删除、选择收货地址、提交订单 |
| 商城 | 收货地址 | `addresses.html` | 地址 CRUD、设为默认 |
| 订单 | 我的订单 | `orders.html` | 统计卡片、状态/日期/关键词筛选；待付款倒计时；付款/取消/确认收货；已取消订单可重新加购 |
| 订单 | 全部订单 | `orders-all.html` | **仅管理员**；查看全部用户订单；发货、退款/退货处理 |
| 订单 | 订单详情 | `order-detail.html` | 订单信息、商品明细、**物流进度时间线（含各节点位置）**、状态操作 |
| 管理 | 商品管理 | `product-admin.html` | **仅管理员**；商品 CRUD、上下架、图片（URL/本地上传） |
| 管理 | 分类管理 | `category-admin.html` | **仅管理员**；分类 CRUD；删除前校验是否有关联商品 |
| 分析 | 订单统计 | `order-stats.html` | **仅管理员**；本月汇总、状态分布、近 7 日趋势 |

**公共脚本**：`js/order-common.js`（侧边栏、状态映射、物流时间线渲染、重新加购等）；`js/orders-list.js`（我的订单/全部订单共用列表逻辑）。

#### 3.3.2 订单状态

| 状态 | 含义 | 典型操作方 |
|------|------|------------|
| `pending` | 待付款 | 用户付款/取消；超时自动取消 |
| `paid` | 待发货（已付款） | 管理员发货/取消 |
| `shipped` | 待收货（已发货） | 用户确认收货 |
| `completed` | 已完成 | 用户可申请退款/退货 |
| `cancelled` | 已取消 | 用户可重新加入购物车 |
| `refunding` / `refunded` | 退款中 / 已退款 | 用户申请；管理员处理 |
| `returning` / `returned` | 退货中 / 已退货 | 用户申请；管理员处理 |

**状态流转要点：**

- 用户下单后状态为 `pending`；**30 分钟**未付款由定时任务自动取消并回补库存（可配置 `order.payment-timeout-minutes`）；
- 结算时扣减库存；待付款取消或退款成功时回补库存；
- 管理员在「全部订单」可发货（`paid` → `shipped`）及处理退款/退货；
- 我的订单列表对待付款订单展示**支付倒计时**。

#### 3.3.3 物流轨迹

- 物流信息存储于独立底表 **`biz_order_logistics`**，与订单 `biz_order` 一对多；
- 每条轨迹含：节点标题、说明、**物流位置**、发生时间、节点状态（`done` / `active` / `wait` / `failed`）、排序号；
- **写入时机**：下单初始化轨迹；订单状态变更时追加/更新预览节点；超时取消时写入取消节点；
- **查询**：订单详情接口返回 `logistics` 数组；若历史订单无底表数据，首次查询时按订单字段**回填并落库**；
- DDL 中为示例订单 id 1–10 预置了初始化物流数据。

#### 3.3.4 商品与分类

- 商品表 `biz_product`：名称、描述、价格、库存、分类、状态（`on_shelf` / `off_shelf`）、图片 URL；
- 分类表 `biz_product_category`：分类名称、排序；商品保存时校验分类有效；
- 非管理员浏览商品列表时**仅展示上架商品**；管理员商品管理可查看全部状态；
- 商品图片支持 **http(s) 外链** 或 **本地上传**（`POST /api/products/upload-image`），静态访问路径 `/uploads/products/**`。

#### 3.3.5 权限

| 能力 | 普通用户 | 管理员 |
|------|----------|--------|
| 浏览上架商品、购物车、结算 | ✓ | ✓ |
| 管理自己的地址与订单 | ✓ | ✓ |
| 商品/分类管理、全部订单、订单统计 | ✗ | ✓ |
| 商品上下架、发货、退款/退货审批 | ✗ | ✓ |

- 需拥有 ORDER_MGMT 子系统 **login** 权限方可进入；
- 管理类写操作：前端 `isAdmin()` + 后端 `@PreAuthorize("hasAnyRole('PLATFORM_ADMIN','SUBSYSTEM_ADMIN')")`（部分接口如购物车、我的订单查询仅需登录）。

#### 3.3.6 订单数据范围

- 列表/详情/统计接口支持参数 `scope`：`mine`（默认，当前用户订单）/ `all`（仅管理员，全部订单）。

### 3.4 数据库管理系统（DB_MGMT）

**定位**：登记并监控 MySQL 数据库实例，支持连接测试与表结构查看。

| 页面 | 路径 | 功能 |
|------|------|------|
| 实例管理 | `index.html` | 实例卡片、搜索、统计；管理员 CRUD、测试连接、单个/全部刷新 |
| 表结构查看 | `tables.html` | 查看实例下表列表、字段结构、DDL 复制 |

**核心逻辑：**

- 实例元数据存储在 `portal_dbmgmt.biz_db_instance`；
- 「刷新」通过 JDBC 实际探测连接状态，并回写在线/离线、表数量；
- 「查看表」对目标实例建立 JDBC 连接，读取 `information_schema`；
- 管理员可新增外部实例（主机、端口、库名、账号密码）。

### 3.5 日志分析平台（LOG_ANALYSIS）

**定位**：集中展示各子系统操作日志，支持多条件检索。

| 页面 | 路径 | 功能 |
|------|------|------|
| 日志查询 | `index.html` | 级别/来源/日期/关键词筛选；动态统计卡片；管理员可删除 |

**日志来源：**

- 后端 `@OperationLog` 注解 + AOP 切面自动写入 `portal_log.sys_operation_log`；
- 记录字段：用户、子系统编码、级别、操作描述、请求方法与 URI、IP、成功/失败、耗时等。

### 3.6 权限模型（核心规则）

#### 3.6.1 预置角色

| 角色编码 | 角色名称 | 说明 |
|----------|----------|------|
| `PLATFORM_ADMIN` | 平台管理员 | 最高权限，可管理所有子系统与用户 |
| `SUBSYSTEM_ADMIN` | 子系统管理员 | 可管理用户及权限分配 |
| `USER` | 普通用户 | 仅使用被分配的权限 |

#### 3.6.2 子系统权限类型

| 类型 | 含义 |
|------|------|
| `login` | 可在门户看到并进入该子系统 |
| `query` | 子系统内查询类操作（与 login 配合使用） |
| `admin` | 子系统内管理类写操作（创建、修改、删除等） |

#### 3.6.3 权限生效规则

1. **直接分配**：`sys_user_subsystem` 为用户直接授予子系统权限；
2. **角色继承**：`sys_role_subsystem` + `sys_user_role`，用户通过角色间接获得权限；
3. **取并集**：两种方式 UNION，任一来源授权即生效；
4. **有效权限视图**：`v_user_effective_permission` 合并上述两种来源，带来源标识（直接分配 / 角色继承）；
5. **门户可见性**：普通用户仅展示有任意权限且 `status=1` 的子系统；
6. **JWT 角色判定**：登录时取用户所有角色中的最高级别（`PLATFORM_ADMIN` > `SUBSYSTEM_ADMIN` > `USER`）；若角色编码仍为 USER 但拥有 `admin` 类型子系统权限，则提升为 `SUBSYSTEM_ADMIN`。

#### 3.6.4 各系统内部权限控制

- **前端**：通过 `isAdmin()` 隐藏/显示写操作按钮；
- **后端**：敏感接口使用 `@PreAuthorize("hasAnyRole('PLATFORM_ADMIN','SUBSYSTEM_ADMIN')")`；
- **认证**：JWT Bearer Token，除 `/api/auth/login`、`/api/auth/register` 外均需认证。

---

## 4. 项目结构

### 4.1 前端项目结构

```
front/
├── login.html                          # 登录页
├── index.html                          # 门户首页（子系统卡片）
├── profile.html                        # 个人信息
├── README.md
├── css/
│   ├── style.css                       # 全局样式
│   └── responsive.css                  # 响应式
├── js/
│   ├── config.js                       # API 地址、Token、request()、Toast 等公共方法
│   └── main.js                         # 门户首页逻辑
└── systems/
    ├── user-management/                # 用户管理子系统
    │   ├── index.html                  # 用户管理
    │   ├── roles.html                  # 角色管理
    │   ├── role-assign.html            # 用户角色分配
    │   ├── user-roles.html             # 查询用户角色
    │   ├── perm-menu.html              # 子系统权限分配
    │   ├── perm-view.html              # 系统权限查看
    │   ├── perm-data.html              # 数据权限（开发中）
    │   ├── css/
    │   └── js/main.js
    ├── order-system/                   # 订单商城子系统
    │   ├── index.html                  # 商品列表
    │   ├── cart.html                   # 购物车
    │   ├── addresses.html              # 收货地址
    │   ├── orders.html                 # 我的订单
    │   ├── orders-all.html             # 全部订单（管理员）
    │   ├── order-detail.html           # 订单详情（含物流时间线）
    │   ├── product-admin.html          # 商品管理（管理员）
    │   ├── category-admin.html         # 分类管理（管理员）
    │   ├── order-stats.html            # 订单统计（管理员）
    │   ├── css/
    │   └── js/
    │       ├── order-common.js         # 公共逻辑
    │       ├── products.js             # 商品列表
    │       ├── cart.js                 # 购物车
    │       ├── addresses.js            # 地址管理
    │       ├── orders-list.js          # 订单列表（我的/全部）
    │       ├── product-admin.js        # 商品管理
    │       └── category-admin.js       # 分类管理
    ├── db-management/                  # 数据库管理子系统
    │   ├── index.html
    │   ├── tables.html
    │   ├── css/
    │   └── js/
    └── log-analysis/                   # 日志分析子系统
        ├── index.html
        ├── css/
        └── js/
```

**前端运行方式：**

- 直接打开 `login.html`，或
- 启动本地静态服务（推荐）：`cd front && python -m http.server 3000`
- 后端 API 默认地址：`http://localhost:8080/api`（在 `js/config.js` 中配置）

### 4.2 后端项目结构

```
backend/
├── pom.xml                             # 父工程 portal-parent
├── config/
│   ├── secrets-example.properties      # 敏感配置模板
│   └── secrets-dev.properties          # 本地开发配置（不入库）
├── portal-common/                      # 公共模块
│   └── 安全(JWT/Security)、通用模型、DTO、操作日志 AOP、CORS 等
├── portal-main/                        # 主模块（启动入口）
│   ├── PortalApplication.java
│   ├── controller/                     # 认证、用户、角色、权限、子系统
│   ├── service/
│   ├── config/                         # 多数据源、密码初始化
│   └── resources/
│       ├── application.yml
│       ├── application-dev.yml
│       ├── application-prod.yml
│       └── DDL.sql                     # 全库 DDL + 初始化数据
├── portal-order/                       # 订单商城业务（商品、购物车、订单、地址、分类、物流轨迹）
├── portal-dbmgmt/                      # 数据库管理子系统业务
└── portal-log/                         # 日志分析子系统业务
```

**模块依赖关系：**

- `portal-main` 聚合启动，依赖 `portal-common`、`portal-order`、`portal-dbmgmt`、`portal-log`；
- 各业务模块仅包含 Controller / Service / Mapper / Model，数据源在 `portal-main` 中统一配置。

**多数据源对应关系：**

| 数据源 | 数据库 | 模块 |
|--------|--------|------|
| mainDataSource | portal_main | portal-main |
| orderDataSource | portal_order | portal-order |
| dbmgmtDataSource | portal_dbmgmt | portal-dbmgmt |
| logDataSource | portal_log | portal-log |

---

## 5. 数据库设计

> 完整脚本见 `backend/portal-main/src/main/resources/DDL.sql`

### 5.1 portal_main（主库）

| 表名 | 说明 |
|------|------|
| `sys_user` | 用户表（密码 BCrypt 加密） |
| `sys_role` | 角色表 |
| `sys_subsystem` | 子系统注册表（名称、编码、图标、颜色、入口 URL、排序、状态） |
| `sys_user_role` | 用户-角色关联 |
| `sys_user_subsystem` | 用户-子系统直接权限 |
| `sys_role_subsystem` | 角色-子系统权限 |
| `v_user_effective_permission` | 用户有效权限视图 |

### 5.2 portal_order（订单库）

| 表名 | 说明 |
|------|------|
| `biz_order` | 订单主表（订单号、用户、收货信息、金额、状态、备注、支付/发货/完成时间等） |
| `biz_order_item` | 订单明细（商品快照：名称、单价、数量、小计） |
| `biz_order_logistics` | 订单物流轨迹（标题、说明、**物流位置**、发生时间、节点状态、排序） |
| `biz_product` | 商品表（名称、描述、价格、库存、分类、状态、图片 URL） |
| `biz_product_category` | 商品分类字典 |
| `biz_cart_item` | 购物车（用户 + 商品 + 数量） |
| `biz_user_address` | 用户收货地址（标签、省市区、详细地址、是否默认） |

**配置项（application.yml）：**

| 配置 | 说明 | 默认 |
|------|------|------|
| `order.payment-timeout-minutes` | 待付款超时自动取消（分钟） | 30 |
| `order.upload-dir` | 商品图片本地上传目录 | uploads/products |
| `order.upload-max-size-mb` | 单张图片大小上限（MB） | 5 |

### 5.3 portal_dbmgmt（数据库管理库）

| 表名 | 说明 |
|------|------|
| `biz_db_instance` | 数据库实例登记表（连接信息、表数量、在线状态等） |

### 5.4 portal_log（日志库）

| 表名 | 说明 |
|------|------|
| `sys_operation_log` | 操作日志表 |

### 5.5 初始化账号

| 用户名 | 初始密码 | 角色 | 说明 |
|--------|----------|------|------|
| admin | admin123 | 平台管理员 | DDL 中为 `INIT:admin123`，首次启动自动 BCrypt 加密 |
| user1 | 123456 | 普通用户 | 示例测试账号，仅有部分子系统 login/query 权限 |

**预置子系统（sys_subsystem）：**

| 编码 | 名称 | 入口 URL |
|------|------|----------|
| USER_MGMT | 用户管理系统 | `/systems/user-management/index.html` |
| ORDER_MGMT | 订单商城 | `/systems/order-system/index.html` |
| DB_MGMT | 数据库管理系统 | `/systems/db-management/index.html` |
| LOG_ANALYSIS | 日志分析平台 | `/systems/log-analysis/index.html` |

---

## 6. 后端 API 概览

> 统一响应格式：`{ "code": 200, "message": "success", "data": ... }`  
> 认证方式：请求头 `Authorization: Bearer <token>`

### 6.1 认证 `/api/auth`

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| POST | `/login` | 登录，返回 JWT | 公开 |
| POST | `/register` | 注册用户 | 公开 |
| GET | `/me` | 当前用户基本信息 | 登录 |
| GET | `/me/detail` | 当前用户详情（含角色、权限） | 登录 |
| PUT | `/profile` | 修改个人信息/密码 | 登录 |

### 6.2 子系统 `/api/subsystems`

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/my` | 当前用户可见子系统列表 |
| GET | `/` | 全部子系统 |
| POST | `/` | 新增子系统 |
| PUT | `/{id}` | 修改子系统 |
| PUT | `/{id}/status` | 启停子系统 |
| DELETE | `/{id}` | 删除子系统 |

### 6.3 用户 `/api/users`

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/` | 分页用户列表 |
| GET | `/all` | 全部用户 |
| GET | `/stats` | 用户统计 |
| GET | `/{id}`、`/{id}/detail` | 用户详情 |
| PUT | `/{id}` | 更新用户（含角色） |
| DELETE | `/{id}` | 删除用户 |
| PUT | `/{id}/status` | 启停用户 |
| PUT | `/{id}/password` | 重置密码 |
| GET | `/{id}/roles` | 用户角色 ID 列表 |
| GET | `/export` | 导出 Excel |
| GET | `/import/template` | 下载导入模板 |
| POST | `/import` | 批量导入 |

### 6.4 角色 `/api/roles`

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/` | 角色列表 |
| POST | `/` | 新增角色 |
| PUT | `/{id}` | 修改角色 |
| PUT | `/{id}/status` | 启停角色 |
| DELETE | `/{id}` | 删除角色 |
| GET | `/{id}/users` | 角色下用户 ID 列表 |
| POST | `/{id}/users` | 批量设置角色用户 |

### 6.5 权限 `/api/permissions` 与 `/api/role-permissions`

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/permissions/grant` | 为用户授予子系统权限 |
| DELETE | `/permissions/revoke` | 撤销用户子系统权限 |
| GET | `/permissions/user/{userId}` | 用户直接权限 |
| GET | `/permissions/user/{userId}/effective` | 用户有效权限（含角色继承） |
| GET | `/role-permissions/role/{roleId}` | 角色子系统权限 |
| POST | `/role-permissions/grant` | 授予角色权限 |
| DELETE | `/role-permissions/revoke` | 撤销角色权限 |

### 6.6 订单 `/api/orders`

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/` | 订单列表（筛选：`keyword`、`status`、`startDate`、`endDate`、`scope=mine\|all`；返回全量 `List`，前端分页） |
| GET | `/stats` | 订单统计（支持 `scope`） |
| GET | `/settings` | 订单相关配置（如支付超时分钟数） |
| GET | `/trend` | 近 7 日趋势 |
| GET | `/month-stats` | 本月统计 |
| GET | `/{id}` | 订单详情（含 `order`、`items`、`logistics` 物流轨迹） |
| POST | `/checkout` | 购物车结算下单 |
| PUT | `/{id}/status` | 变更订单状态 |
| POST | `/{id}/reorder-to-cart` | 已取消订单商品重新加入购物车 |

### 6.7 商品 `/api/products`

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/` | 商品列表（筛选：`keyword`、`status`、`category`；返回全量 `List`，前端分页） |
| GET | `/categories` | 有效分类名称列表 |
| GET | `/{id}` | 商品详情 |
| POST | `/` | 新增商品（管理员） |
| PUT | `/{id}` | 更新商品（管理员） |
| PUT | `/{id}/status` | 上下架（管理员） |
| DELETE | `/{id}` | 删除商品（管理员） |
| POST | `/upload-image` | 上传商品图片（管理员，multipart） |

### 6.8 商品分类 `/api/product-categories`

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/` | 分类列表（支持 `keyword` 筛选） |
| GET | `/{id}` | 分类详情 |
| POST | `/` | 新增分类（管理员） |
| PUT | `/{id}` | 更新分类（管理员；重命名会同步更新关联商品分类字段） |
| DELETE | `/{id}` | 删除分类（管理员；有关联商品时拒绝） |

### 6.9 购物车 `/api/cart`

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/` | 当前用户购物车明细 |
| GET | `/count` | 购物车商品总件数 |
| POST | `/` | 加入购物车（body: `productId`, `quantity`） |
| PUT | `/{id}` | 修改数量 |
| DELETE | `/{id}` | 移除单项 |
| DELETE | `/` | 清空购物车 |

### 6.10 收货地址 `/api/addresses`

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/` | 当前用户地址列表 |
| GET | `/{id}` | 地址详情 |
| POST | `/` | 新增地址 |
| PUT | `/{id}` | 更新地址 |
| DELETE | `/{id}` | 删除地址 |
| PUT | `/{id}/default` | 设为默认地址 |

### 6.11 数据库 `/api/db`

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/instances` | 实例列表 |
| GET | `/stats` | 汇总统计 |
| POST | `/instances/refresh` | 批量刷新状态 |
| POST | `/instances/{id}/refresh` | 刷新单个实例 |
| POST | `/instances/test-connection` | 测试连接 |
| GET/POST/PUT/DELETE | `/instances[...]` | 实例 CRUD |
| GET | `/instances/{id}/tables` | 表列表 |
| GET | `/instances/{id}/tables/{tableName}/structure` | 表结构 |
| GET | `/instances/{id}/tables/{tableName}/ddl` | 表 DDL |
| GET | `/instances/{id}/realstats` | 实例实时统计 |

### 6.12 日志 `/api/logs`

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/all` | 日志列表（支持 level、subsystemCode、keyword、日期范围） |
| GET | `/subsystem-codes` | 日志中出现的子系统编码 |
| GET | `/stats` | 今日统计 |
| DELETE | `/{id}` | 删除日志 |

---

## 7. 部署与运行

### 7.1 数据库初始化

1. 安装 MySQL 5.7；
2. 执行 `backend/portal-main/src/main/resources/DDL.sql`，创建 4 个库及初始数据。

### 7.2 后端启动

1. 复制 `backend/config/secrets-example.properties` 为 `secrets-dev.properties`，填写数据库密码与 JWT 密钥；
2. 在 `backend` 目录执行：
   ```bash
   mvn clean package -DskipTests
   java -jar portal-main/target/portal-main-1.0.0.jar
   ```
3. 默认端口：`8080`，环境切换通过 `spring.profiles.active`（dev / prod）。

### 7.3 前端访问

1. 启动静态服务或直接打开 `front/login.html`；
2. 使用 admin / admin123 登录门户；
3. 从门户卡片进入各子系统。

---

## 8. 待完善功能

| 功能 | 状态 | 说明 |
|------|------|------|
| 数据权限管理 | 开发中 | `perm-data.html` 为占位页，尚无后端接口 |
| 子系统级管理员隔离 | 部分实现 | 当前子系统 admin 权限以前端隐藏 + 后端角色校验为主，尚未按子系统维度细粒度隔离管理员可操作范围 |

---

## 9. 技术要点摘要

- **认证**：Spring Security + JWT（jjwt），无 Session，Token 有效期默认 24 小时；
- **ORM**：MyBatis Plus，多数据源手动配置（排除 Spring Boot 自动数据源）；
- **操作日志**：AOP 切面 `@OperationLog` 异步写入日志库；
- **密码**：BCrypt 加密；DDL 初始密码使用 `INIT:` 前缀，启动时自动迁移；
- **跨域**：CorsConfig 允许前端静态站点跨域访问 API；
- **Excel**：Apache POI 实现用户导入导出（portal-main 模块）；
- **订单商城**：独立数据源 `portal_order`；购物车结算扣库存；`OrderTimeoutScheduler` 定时取消超时未付款订单；商品图片静态资源映射 `/uploads/products/**`；物流轨迹持久化于 `biz_order_logistics`。
